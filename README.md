# Music-listener-segmentation
Unsupervised segmentation of music listeners into behavioral groups (Casual, Explorer, Heavy) using K-Means clustering, deployed as an interactive Streamlit app for real-time segment prediction.
